package org.firstinspires.ftc.teamcode.opModes.auto;

public class twelveArtifactPath {
    /*
    public class GeneratedPaths {

  public static PathBuilder builder = new PathBuilder();

  public static PathChain line1 = builder
    .addPath(
      new BezierLine(new Pose(63.830, 135.830), new Pose(56.000, 96.000))
    )
    .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(140))
    .build();

  public static PathChain line2 = builder
    .addPath(
      new BezierCurve(
        new Pose(56.000, 96.000),
        new Pose(53.617, 81.191),
        new Pose(18.000, 84.000)
      )
    )
    .setLinearHeadingInterpolation(Math.toRadians(140), Math.toRadians(180))
    .build();

  public static PathChain line3 = builder
    .addPath(new BezierLine(new Pose(18.000, 84.000), new Pose(60.000, 83.000)))
    .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(135))
    .build();

  public static PathChain line4 = builder
    .addPath(
      new BezierCurve(
        new Pose(60.000, 83.000),
        new Pose(59.234, 58.213),
        new Pose(18.000, 60.000)
      )
    )
    .setLinearHeadingInterpolation(Math.toRadians(135), Math.toRadians(180))
    .build();

  public static PathChain line5 = builder
    .addPath(new BezierLine(new Pose(18.000, 60.000), new Pose(60.000, 83.000)))
    .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(135))
    .build();

  public static PathChain line6 = builder
    .addPath(
      new BezierCurve(
        new Pose(60.000, 83.000),
        new Pose(59.234, 30.128),
        new Pose(18.000, 36.000)
      )
    )
    .setLinearHeadingInterpolation(Math.toRadians(135), Math.toRadians(180))
    .build();

  public static PathChain line7 = builder
    .addPath(new BezierLine(new Pose(18.000, 36.000), new Pose(60.000, 83.000)))
    .setLinearHeadingInterpolation(Math.toRadians(180), Math.toRadians(135))
    .build();

  public static PathChain line8 = builder
    .addPath(new BezierLine(new Pose(60.000, 83.000), new Pose(28.000, 72.000)))
    .setLinearHeadingInterpolation(Math.toRadians(135), Math.toRadians(180))
    .build();
}
     */
}
